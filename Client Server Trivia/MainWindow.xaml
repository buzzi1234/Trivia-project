<Window x:Class="Client_Server_Trivia.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client_Server_Trivia"
        mc:Ignorable="d"
        Title="Trivia Game - Login" 
        Height="700" Width="965"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Setting gradients and horizons -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#667eea" Offset="0"/>
            <GradientStop Color="#764ba2" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="CardGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFFFFF" Offset="0"/>
            <GradientStop Color="#F8F9FA" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="PrimaryColor" Color="#667eea"/>
        <SolidColorBrush x:Key="AccentColor" Color="#764ba2"/>

        <!--button design -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#667eea" Offset="0"/>
                        <GradientStop Color="#764ba2" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25" BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#667eea" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#5a6fd8" Offset="0"/>
                                            <GradientStop Color="#6942a1" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Format text fields -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E6ED"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#667eea"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Password field design -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E6ED"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,12,45,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#667eea"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- עיצוב כפתור עין -->
        <Style x:Key="EyeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#718096"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#667eea"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- animations -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.8"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="30" To="0" Duration="0:0:0.8">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
        </EventTrigger>
    </Window.Triggers>

    <Grid Background="{StaticResource BackgroundGradient}" Margin="0,0,215,-55">
        <Grid.RenderTransform>
            <TranslateTransform/>
        </Grid.RenderTransform>

        <!-- Close button -->
        <Button Name="CloseButton" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,15,0" Width="30" Height="30" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="16" Content="✕" Cursor="Hand" Click="CloseButton_Click"/>

        <!-- Decorative background -->
        <Ellipse Width="300" Height="300" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-150,-150,0,0" Fill="#FFFFFF" Opacity="0.1"/>
        <Ellipse Width="200" Height="200" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-100,-100" Fill="#FFFFFF" Opacity="0.1"/>
        <Ellipse Width="300" Height="300" HorizontalAlignment="Left" VerticalAlignment="Top" Fill="#FFFFFF" Opacity="0.1" Margin="90,300,0,0"/>
        <Ellipse Width="300" Height="300" HorizontalAlignment="Left" VerticalAlignment="Top" Fill="#FFFFFF" Opacity="0.1" Margin="595,100,0,0"/>

        <!-- main tab -->
        <Border Background="{StaticResource CardGradient}" CornerRadius="20" Width="450" Height="656" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="10" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel Margin="50,40">
                <!-- סמל מגניב של לוגין -->
                <Viewbox Width="80" Height="80" Margin="0,0,0,20">
                    <Canvas Width="100" Height="100">
                        <!-- רקע עיגול -->
                        <Ellipse Width="100" Height="100" Fill="#667eea" Opacity="0.1"/>
                        
                        <!-- מגן עם מנעול -->
                        <Path Fill="#667eea" 
                              Data="M20,35 L80,35 L80,85 L20,85 Z M50,15 C58,15 65,22 65,30 L65,35 L35,35 L35,30 C35,22 42,15 50,15 Z M42,30 C42,26 46,22 50,22 C54,22 58,26 58,30 L58,35 L42,35 Z"/>
                        
                        <!-- מנעול פנימי -->
                        <Ellipse Width="12" Height="12" Canvas.Left="44" Canvas.Top="52" Fill="white"/>
                        <Rectangle Width="3" Height="8" Canvas.Left="48.5" Canvas.Top="58" Fill="white"/>
                        
                        <!-- אפקט ברק -->
                        <Path Fill="#764ba2" Opacity="0.7"
                              Data="M15,15 L25,25 L20,30 L30,40 L20,35 L25,45 L15,35 L20,30 L10,20 Z"/>
                        <Path Fill="#764ba2" Opacity="0.7"
                              Data="M75,65 L85,75 L80,80 L90,90 L80,85 L85,95 L75,85 L80,80 L70,70 Z"/>
                    </Canvas>
                </Viewbox>

                <!-- title -->
                <TextBlock Text="TRIVIA MASTER" FontSize="36" FontWeight="Bold" Foreground="#2D3748" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <TextBlock Text="Welcome!" FontSize="28" FontWeight="SemiBold" Foreground="#4A5568" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Login to start playing" FontSize="16" Foreground="#718096" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <!-- input fields -->
                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="Username" FontSize="14" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,0,0,8"/>
                    <TextBox Name="UsernameTextBox" Style="{StaticResource ModernTextBox}" Height="45"/>

                    <TextBlock Text="Password" FontSize="14" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,20,0,8"/>
                    <Grid>
                        <PasswordBox Name="PasswordBox" Style="{StaticResource ModernPasswordBox}" Height="45"/>
                        <TextBox Name="PasswordTextBox"
                                 Style="{StaticResource ModernTextBox}"
                                 Height="45"
                                 Visibility="Collapsed"/>

                    </Grid>
                </StackPanel>

                <!-- buttons -->
                <Button Name="LoginButton" Content="Login" Style="{StaticResource ModernButton}" Height="50" Margin="0,0,0,15" Click="LoginButton_Click"/>

                <Button Name="SignupButton" Content="Don't have an account? Sign up here" Background="Transparent" Foreground="{StaticResource PrimaryColor}" BorderThickness="0" FontSize="14" Cursor="Hand" Click="SignupButton_Click"/>

                <!-- error message -->
                <TextBlock Name="ErrorMessageTextBlock" Text="" Foreground="#E53E3E" FontSize="12" HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>