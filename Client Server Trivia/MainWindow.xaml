<Window x:Class="Client_Server_Trivia.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.cc:\users\cyber_user\documents\magshimim\trivia\triv\trivia-project\client server trivia\mainwindow.xaml.cs
c:\users\cyber_user\documents\magshimim\trivia\triv\trivia-project\client server trivia\mainwindow.xaml
om/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client_Server_Trivia"
        mc:Ignorable="d"
        Title="Trivia Game - Login" 
        Height="828" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">

    <Window.Resources>
        <!-- Setting gradients and horizons -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#ff9a9e" Offset="0"/>
            <GradientStop Color="#fecfef" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="CardGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFFFFF" Offset="0"/>
            <GradientStop Color="#F8F9FA" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="PrimaryColor" Color="#ff6b6b"/>
        <SolidColorBrush x:Key="AccentColor" Color="#ee5a24"/>

        <!--button design -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#ff6b6b" Offset="0"/>
                        <GradientStop Color="#ee5a24" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25" BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#ff6b6b" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#ff5252" Offset="0"/>
                                            <GradientStop Color="#d63031" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Format text fields -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E6ED"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#ff6b6b"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Password field design -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E6ED"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,12,45,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#ff6b6b"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- עיצוב כפתור עין -->
        <Style x:Key="EyeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#718096"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#667eea"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- animations -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.8"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="30" To="0" Duration="0:0:0.8">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
        </EventTrigger>
    </Window.Triggers>

    <Grid Background="{StaticResource BackgroundGradient}" Margin="0,82,0,-57">
        <Grid.RowDefinitions>
            <RowDefinition Height="321*"/>
            <RowDefinition Height="359*"/>
        </Grid.RowDefinitions>
        <Grid.RenderTransform>
            <TranslateTransform/>
        </Grid.RenderTransform>

        <!-- Close button -->
        <Button Name="CloseButton" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,15,0" Width="30" Height="30" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="16" Content="✕" Cursor="Hand" Click="CloseButton_Click"/>

        <!-- Decorative background -->
        <Ellipse Width="300" Height="300" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-224,-70,0,0" Fill="#FFFFFF" Opacity="0.1"/>
        <Ellipse Width="200" Height="200" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-100,-100" Fill="#FFFFFF" Opacity="0.1" Grid.Row="1"/>
        <Ellipse Width="300" Height="300" HorizontalAlignment="Left" VerticalAlignment="Top" Fill="#FFFFFF" Opacity="0.1" Margin="-230,289,0,0" Grid.RowSpan="2"/>
        <Ellipse Width="300" Height="300" HorizontalAlignment="Left" VerticalAlignment="Top" Fill="#FFFFFF" Opacity="0.1" Margin="526,25,0,0" Grid.RowSpan="2"/>

        <!-- main tab -->
        <Border Background="{StaticResource CardGradient}" CornerRadius="20" Width="442" Height="603" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,25,0,0" Grid.RowSpan="2">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="10" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel Margin="50,40">
                <!-- New Logo -->
                <Viewbox Width="80" Height="80" Margin="0,0,0,20">
<<<<<<< HEAD
                    <Canvas Width="120" Height="120">
                        <Ellipse Width="120" Height="120" Fill="#ff6b6b" Opacity="0.1"/>
                        <Rectangle Width="35" Height="50" Canvas.Left="25" Canvas.Top="35" 
                                 Fill="#667eea" RadiusX="5" RadiusY="5" Stroke="White" StrokeThickness="2"/>
                        <Rectangle Width="25" Height="15" Canvas.Left="30" Canvas.Top="45" 
                                 Fill="White" RadiusX="2" RadiusY="2"/>
                        <Rectangle Width="25" Height="8" Canvas.Left="30" Canvas.Top="55" 
                                 Fill="White" RadiusX="2" RadiusY="2"/>
                        <Rectangle Width="35" Height="50" Canvas.Left="60" Canvas.Top="35" 
                                 Fill="White" RadiusX="5" RadiusY="5" Stroke="#ff6b6b" StrokeThickness="2"/>
                        <Path Canvas.Left="72" Canvas.Top="50" Fill="#ff6b6b" 
                              Data="M0,5 C0,2 2,0 5,0 C8,0 10,2 10,5 C10,8 5,15 5,15 S0,8 0,5 Z"/>
                        <TextBlock Canvas.Left="63" Canvas.Top="37" Text="A" FontSize="8" 
                                 FontWeight="Bold" Foreground="#ff6b6b"/>
                        <Path Fill="#ffd700" Canvas.Left="15" Canvas.Top="20" 
                              Data="M5,0 L6,3 L10,3 L7,5 L8,9 L5,7 L2,9 L3,5 L0,3 L4,3 Z"/>
                        <Path Fill="#ffd700" Canvas.Left="95" Canvas.Top="25" 
                              Data="M3,0 L4,2 L6,2 L4,3 L5,6 L3,4 L1,6 L2,3 L0,2 L2,2 Z"/>
                        <Path Fill="#ffd700" Canvas.Left="85" Canvas.Top="90" 
                              Data="M4,0 L5,2 L8,2 L6,4 L7,7 L4,5 L1,7 L2,4 L0,2 L3,2 Z"/>
=======
                    <Canvas Width="100" Height="100">
                        <!-- רקע עיגול -->
                        <Ellipse Width="100" Height="100" Fill="#667eea" Opacity="0.1"/>

                        <!-- מגן עם מנעול -->
                        <Path Fill="#667eea" 
                              Data="M20,35 L80,35 L80,85 L20,85 Z M50,15 C58,15 65,22 65,30 L65,35 L35,35 L35,30 C35,22 42,15 50,15 Z M42,30 C42,26 46,22 50,22 C54,22 58,26 58,30 L58,35 L42,35 Z"/>

                        <!-- מנעול פנימי -->
                        <Ellipse Width="12" Height="12" Canvas.Left="44" Canvas.Top="52" Fill="white"/>
                        <Rectangle Width="3" Height="8" Canvas.Left="48.5" Canvas.Top="58" Fill="white"/>

                        <!-- אפקט ברק -->
                        <Path Fill="#764ba2" Opacity="0.7"
                              Data="M15,15 L25,25 L20,30 L30,40 L20,35 L25,45 L15,35 L20,30 L10,20 Z"/>
                        <Path Fill="#764ba2" Opacity="0.7"
                              Data="M75,65 L85,75 L80,80 L90,90 L80,85 L85,95 L75,85 L80,80 L70,70 Z"/>
>>>>>>> 7bb1dbf3a813ccb0e7945ba7e1b3f227031a4e49
                    </Canvas>
                </Viewbox>

                <!-- title -->
                <TextBlock Text="TRIVIA MASTER" FontSize="36" FontWeight="Bold" Foreground="#2D3748" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <TextBlock Text="Welcome!" FontSize="28" FontWeight="SemiBold" Foreground="#4A5568" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Login to start playing" FontSize="16" Foreground="#718096" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- input fields -->
                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="Username" FontSize="14" FontWeight="SemiBold" Foreground="#4A5568"/>
                    <TextBox x:Name="UsernameTextBox" Style="{StaticResource ModernTextBox}" Height="45" Foreground="black" TextChanged="UsernameTextBox_TextChanged" Text="" FontFamily="Rockwell"/>

                    <TextBlock Text="Password" FontSize="14" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,20,0,8"/>
                    <Grid>
                        <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordBox}" Height="45"/>
                        <TextBox x:Name="PasswordTextBox"
                                 Style="{StaticResource ModernTextBox}"
                                 Height="45"
                                 Visibility="Hidden"/>

                    </Grid>
                </StackPanel>

                <!-- buttons -->
                <Button Name="LoginButton" Content="Login" Style="{StaticResource ModernButton}" Height="50" Margin="0,0,0,15" Click="LoginButton_Click"/>

                <Button Name="SignupButton" Content="Don't have an account? Sign up here" Background="Transparent" Foreground="{StaticResource PrimaryColor}" BorderThickness="0" FontSize="14" Cursor="Hand" Click="SignupButton_Click"/>

                <!-- error message -->
                <TextBlock Name="ErrorMessageTextBlock" Text="" Foreground="#E53E3E" FontSize="12" HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>